---
title: 'MYSQL5.7安装步骤'
date: '2022-09-12 14:01'
tags: ['MySQL', 'Database']
draft: false
layout: PostLayout
summary: 'MYSQL5.7安装包括镜像下载以及初始化过程,卸载MYSQL以及残留'
---

#### 卸载MYSQL以及残留


1. 删除MYSQL隐藏文件：C盘的 ProgramData 下会有一个 MySQL 文件夹，这个文件夹要删除

2. 删除注册表：  
Windows运行框中输入 regedit , 打开注册表编辑窗口，逐级打开找到如下项 ：
HKEY_LOCAL_MACHINE\SYSTEM\ControlSet001\Services\Eventlog\Application\MySQL
HKEY_LOCAL_MACHINE\SYSTEM\ControlSet002\Services\Eventlog\Application\MySQL
HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Services\Eventlog\Application\MySQL


3. 删除服务项：运行框中运行 CMD 打开 Windows 命令行窗口，执行如下命令 : sc delete MySQL服务名 (比如 mysql57, 可以在本机服务列表中查看具体名称)




#### 镜像站

* [清华大学镜像站](https://mirrors.tuna.tsinghua.edu.cn/)
* [阿里云镜像站](https://developer.aliyun.com/mirror/)
* [网易镜像](http://mirrors.163.com/)



#### 使用清华镜像包安装
##### Mysql5.7安装包下载

下载64位MSI安装文件之后,点击安装。安装时选择安装的位置。


##### 环境变量配置

在系统变量中Path中添加Mysql安装目录,一定要添加到bin目录级
如果Path值过长无法保存，可修改原Path变量名为Path1,新建Path变量，添加**%Path1%**



##### MySQL服务的安装与初次登录


1.以管理员启动命令窗
2.进入到MYSQL安装目录(bin级)
3.执行命令
```JS
mysqld -install
-- 输出 Service successfully installed 

mysqld --initialize 
-- 没有输出，重新回到光标
```
4.两条命令执行完毕以后，在MySQL的安装目录下会出现一个data文件夹,在data文件夹中有一个以.err结尾的文件,文件中存储了数据库的临时密码。也可以使用记事本的查找功能查找关键词password。记住此密码！！！
5.执行命令 **net start mysql** ，启动MySQL服务
6.初次登录：
```sql
mysql -u root -p
```
7.修改密码
```sql
alter user 'root'@'localhost' identified by '123456'
```
8.使用quit命令退出MySQL
9.在MYSQL安装目录下新建my.ini文件
```xml
[client]
default-character-set=utf8

[mysql]
default-character-set=utf8

[mysqld]
character-set-server=utf8
```
10.以管理员身份启动命令行，重启MySQL服务
```SQL
net stop mysql
net start mysql
```
11.登录MySQL，使用命令 status 查看字符集。由于配置了环境变量，直接使用普通命令行即可登录MySQL





